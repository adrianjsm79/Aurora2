<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos existentes -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Permisos para Localización -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Requerido para Android 10 (API 29) y superior para acceder a la ubicación en segundo plano -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Permisos para Servicios en Primer Plano y Notificaciones -->
    <!-- Requerido para apps que apuntan a Android 9 (API 28) y superior -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Requerido para Android 13 (API 33) y superior para mostrar notificaciones -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />


    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_aurora"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_aurora"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aurora">

        <!-- Declaración del Servicio de Rastreo -->
        <service
            android:name=".service.TrackingService"
            android:foregroundServiceType="location"
            android:exported="false"/>

        <activity
            android:name=".ui.activities.DevicesActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.SecurityActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.LocationActivity"
            android:exported="false" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBijyV64POjqztyTFwbHH7D_L1W4aeIxHs" />

        <activity
            android:name=".ui.activities.SearchmapActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.TestActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.ContactsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.BaseActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>