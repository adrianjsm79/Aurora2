<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 1. EL MAPA (Fondo) -->
    <fragment
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="80dp" /> <!-- Margen para que se vea algo del mapa con el sheet -->

    <!-- 2. BOTÓN FLOTANTE DE CONTROL DE UBICACIÓN (Arriba Derecha) -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/card_location_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="48dp"
        android:layout_marginEnd="16dp"
        app:cardCornerRadius="24dp"
        app:cardElevation="6dp">

        <LinearLayout
            android:id="@+id/btn_toggle_location"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackground"
            android:clickable="true"
            android:focusable="true"
            android:gravity="center_vertical"
            android:paddingHorizontal="16dp"
            android:paddingVertical="10dp">

            <!-- El icono cambia según estado -->
            <ImageView
                android:id="@+id/icon_location_status"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@drawable/ic_location"
                app:tint="@color/black"/>

            <TextView
                android:id="@+id/text_location_status"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:fontFamily="@font/k2d_bold"
                android:text="Activar GPS"
                android:textColor="@color/black"
                android:textSize="14sp" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- 3. BOTÓN ATRÁS (Arriba Izquierda) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_margin="16dp"
        android:layout_marginTop="48dp"
        android:src="@drawable/ic_arrow_left"
        app:backgroundTint="?attr/colorSurface"
        app:tint="?attr/colorOnSurface"
        app:fabSize="mini"
        android:contentDescription="Volver"/>

    <!-- 4. BOTTOM SHEET (Lista de Dispositivos) -->
    <LinearLayout
        android:id="@+id/bottom_sheet"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_bottom_sheet_rounded"
        android:orientation="vertical"
        android:paddingTop="8dp"
        app:behavior_hideable="false"
        app:behavior_peekHeight="130dp"
        app:layout_behavior="com.google.android.material.bottomsheet.BottomSheetBehavior">

        <!-- Manija de arrastre -->
        <View
            android:layout_width="40dp"
            android:layout_height="4dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="12dp"
            android:background="@drawable/shape_drag_handle"
            android:backgroundTint="#DDDDDD"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Dispositivos en Mapa"
            android:textSize="16sp"
            android:fontFamily="@font/k2d_bold"
            android:textColor="?attr/colorOnSurface"
            android:layout_marginHorizontal="20dp"
            android:layout_marginBottom="12dp"/>

        <!-- Lista -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/map_devices_recycler"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clipToPadding="false"
            android:paddingBottom="20dp"
            tools:itemCount="3"
            tools:listitem="@layout/item_map_device"/>

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>